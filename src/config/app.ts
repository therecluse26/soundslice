interface AppConfig {
    name: string,
    author: {
        name: string,
        url: string
    },
}

export const appConfig: AppConfig = {
    name: "SoundSlice",
    author: {
        name: "therecluse26",
        url: "https://github.com/therecluse26",
    }
}